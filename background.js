// This file currently doesn't have specific functionality.
// You can use it to manage background processes or messaging if needed.

chrome.runtime.onInstalled.addListener(() => {
    console.log("XPath Finder Extension installed.");
});
